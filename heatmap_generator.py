import numpy as np\nimport matplotlib.pyplot as plt\nimport plotly.express as px\n\n# Function to create a standard heatmap using matplotlib\ndef plot_heatmap_matplotlib(data, x_labels, y_labels, title):\n    plt.figure(figsize=(10, 8))\n    plt.imshow(data, cmap='hot', interpolation='nearest')\n    plt.colorbar(label='Intensity')\n    plt.xticks(np.arange(len(x_labels)), x_labels, rotation=45)\n    plt.yticks(np.arange(len(y_labels)), y_labels)\n    plt.title(title)\n    plt.xlabel('X-axis')\n    plt.ylabel('Y-axis')\n    plt.tight_layout()\n    plt.show()\n\n# Function to create an interactive heatmap using plotly\ndef plot_heatmap_plotly(data, x_labels, y_labels, title):\n    fig = px.imshow(data, labels=dict(x='X-axis', y='Y-axis', color='Intensity'),\n                    x=x_labels, y=y_labels, title=title, color_continuous_scale='hot')\n    fig.show()\n\n# Example usage\nif __name__ == '__main__':\n    # Sample data\n    data = np.random.rand(10,10)  # 10x10 matrix\n    x_labels = [f'X{i}' for i in range(10)]\n    y_labels = [f'Y{i}' for i in range(10)]\n    \n    # Plot with matplotlib\n    plot_heatmap_matplotlib(data, x_labels, y_labels, 'Heatmap Visualization using Matplotlib')\n    \n    # Plot with plotly\n    plot_heatmap_plotly(data, x_labels, y_labels, 'Interactive Heatmap Visualization using Plotly')\n