import numpy as np
import cv2

def generate_heatmap(image, attention_map):
    # Convert attention map to heatmap using cv2.applyColorMap
    # Overlay on original image
    return "heatmap_path.png"
